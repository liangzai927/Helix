{
  "name": "@helix-agent/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop adapter workspace package for Helix Agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run --passWithNoTests"
  }
}
