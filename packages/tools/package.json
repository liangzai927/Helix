{
  "name": "@helix-agent/tools",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling workspace package for Helix Agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@helix-agent/protocol": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run --passWithNoTests"
  }
}
